# 认证功能

为进一步保障数据安全，新增认证功能，支持通过配置开启/禁用。更具体地：

1. 支持多个 token 认证
2. token 区分读、写接口, 分开设置
3. 通过在请求 header 中携带 token

# 管理工具

1. 提供管理工具，登录容器运行，而非接口
2. 功能：统计文件信息（总文件数、总大小，分用户的文件数、文件大小）；文件管理（清除所有/指定用户文件，同时支持指定有效期，比如请求全部用户或某个用户的 x 天前的文件）
3. 添加其他必要的功能

# 其他

1. 生成的随机文件名使用 uuid 即可
